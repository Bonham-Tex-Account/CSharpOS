---
name: project-spawning-filesystem-plans
description: "Roadmap: step 1 (I/O device table+fds) DONE & committed 2eaae80 (397 tests); steps 2-4 (disk, focus console, spawning) planned; plus branch-prediction + paging plans. Build order + locked decisions; plan files under ~/.claude/plans/."
metadata:
  node_type: memory
  type: project
  originSessionId: d12a9247-8389-4eba-bf6d-9aafa8359b39
---

Design plans saved as files; **step 1 is implemented, steps 2–4 + two extra features are planned** (as of 2026-06-25). The I/O work was split into a structural half and a policy half so the disk can be a first-class device. **Build order:**

1. **I/O device table + fds** (mechanism half) — plan `io-device-abstraction.md`. ✅ **DONE 2026-06-25 — committed `2eaae80` on branch ShellBacklog (`dotnet test` = 397 passing = 386 baseline + 11 new).** First-class `Device` table (input queue + **wait queue** + output-busy); **per-process fd table** (fd0=stdin, fd1=stdout) — `IN`/`OUT` resolve via the fd's device; `KernelInput` blocks by appending to the device's wait queue; the C# interrupt dispatch wakes the first waiter (FIFO), reusing the unchanged ISA wake routine. Behavior-preserving via a `device==process-index` shim. **As built:** `CSharpOS/CPU/Device.cs` added; the entry constants + `ProcessEntrySize` 160→**176** + fd table at offset 160 live in **`Hardware.cs`** (NOT `Hardware.Types.cs`, which only holds the `Interrupt` struct); `DiskDeviceId = 256` reserved; helpers `FdDevice`/`AddInputWaiter`/`NextInputWaiter`; new tests `DeviceTableTests`+`FdTableTests`. **Gap for step 3 (CLOSED in step 3):** the device-as-process wake fallback was removed so input wakes only real registered waiters — see the step-3 note below.
2. **Bin filesystem disk** — registered as a block device on that table. Plan: `bin-filesystem-prototype.md`. ✅ **DONE 2026-06-26 (implemented, NOT yet committed) on branch ShellBacklog — `dotnet test` = 431 passing.** As built: `CSharpOS/Disk/Bin.cs` (flat slot store, locked API); `Device.Block` (`Bin?`) field + `Device(int,Bin)` ctor; `Hardware` got a 2nd ctor `(…, Bin disk)` (the old ctor delegates with a default `Bin(DefaultDiskSlots=64, DefaultDiskSlotSize=1024)`), registers a block device at `DiskDeviceId` in the ctor, exposes `hw.Disk`, and `DiskRead`/`DiskWrite` transfer methods. `DREAD=0x44`/`DWRITE=0x45` privileged (in-handler `User`-mode guard → `TrapInvalidInstruction`, NOT a trap provider); wired in Instruction/InstructionFunctions/Assembler(`DRead`/`DWrite`)/Disassembler. `Process.ProgramSlot=-1` + `Process(int slot,int,int)` ctor (file-path ctor kept). `ProcessEntryDiskSlot=152` in Hardware.cs. **Load split into two ISA routines (design fix — first attempt fused alloc+DREAD into `IvtLoadProcess` and needed a `DiskSlot<0` skip sentinel, which is gone):** `IvtAllocate=8` = pure buddy allocator (disk-free, reusable — paging/fork will want it), `IvtDiskLoad=9` = standalone routine (`EmitDiskLoad`) that reads the entry's ProgramAddress+DiskSlot and DREADs slot→RAM (EBX=entry, R9=dest, R10=slot, R11=lenOut). `IvtLoadProcess` is **removed**; IVT grew to `IvtSlotCount=10` (CodeBase 36→40). `OperatingSystem.LoadProcess` resolves/auto-stages the slot, reads length via `hw.Disk.GetLength`, seeds the slot, then dispatches `IvtAllocate` and — only if ProgramAddress≥0 — `IvtDiskLoad` (two synchronous dispatches); the C# image `WriteBytes` is gone. Allocator-isolation tests (`OsAllocatorRoutineTests`) target `IvtAllocate` and need no disk seeding. Console (`Program.cs`) reworked to slot-based: builds bytes, `hw.Disk.Store`, `new Process(slot,…)` via a `StagedProgram` type + `StageAll`; temp-file `WriteProgram`/`programDir` removed. New tests `BinTests`+`DiskInstructionTests` + 2 load-from-disk integration tests in `OperatingSystemTests`. 431 tests.
3. **Foreground/focus + realistic console** (policy half) — plan `io-active-process-routing.md`. ✅ **DONE 2026-06-26 (implemented, NOT yet committed) on branch ShellBacklog — `dotnet test` = 439 passing.** As built: **Hardware** `activeProcess` field + `SetActiveProcess`/`GetActiveProcess` (default -1; supersedes spawning's `ActiveProcessOffset`); `RaiseInputInterrupt(value)` (no-device) now routes to the focused process's stdin device via new `FocusedInputDevice()` (reads activeProcess entry's fd[StdIn]; falls back to device 0 when activeProcess<0 — so the broad existing test surface using the no-arg overload is unchanged); per-device `RaiseInputInterrupt(value,deviceId)` kept. `ProgramOutputArgs` gained `SourceProcess` (= running index via `CurrentDeviceId()`). **Wake routing made explicit (closed the step-1 gap, as the tail of step 3):** removed the device-as-process fallback in `NextInputWaiter` — `TryDispatchPendingInterrupt`'s InputReady path now dispatches `IvtWakeInput` only when the device has a real registered waiter (`device.Waiters.Count > 0`); otherwise the value just stays buffered until a process reads it (input-before-block was already handled by buffering, so this is safe). `NextInputWaiter(device)` lost its fallback param. Only `OsRunLoopTests.Run_WhenInterruptPending...` leaned on the old no-op fallback (fixed by registering a waiter via `hw.GetDevice(0).Waiters.Add(0)` — the precondition KernelInput sets); added a lock-in test that a no-waiter interrupt buffers without entering a wake routine. NB: the OUTPUT completion path still wakes by `interrupt.Device` (device-as-process) — output has no waiter queue; out of scope, untouched. Part 4 (`validated-booping-koala.md`) explicitly scopes I/O routing OUT ("done by the I/O efforts, steps 1 & 3"), so this had to be closed here, not in part 4. **Console:** DELETED `ConsoleWindowTerminal`/`TerminalHost`/`IProcessTerminal`/`ProcessIoRouter` + the `--terminal` mode; `Program.cs` reworked (no windows/router/useWindows; `RunShared` + generalized `Run`). `VisualizerModel` gained `FocusedProcess` + per-process `OutputBuffers` (`RecordOutput`/`FocusedOutput`); `HardwareEventBridge.OnProgramOutput` buffers by source. `InteractionController` grew Tab(cycle focus)/digits/Enter(submit number)/Backspace — **Enter no longer steps forward (Right still does)**; ctor now takes `cycleFocus`+`submitInput` callbacks. `SpectreDashboard` owns focus (auto-focus first live, Tab cycles, advances on terminate via `EnsureFocus`), a new **Screen** panel (middle row: procs|buddy|screen) showing the focused buffer + input line, and now drives `RaiseOutputComplete` itself (the retired router's job). Tests: deleted `ProcessIoRouterTests`; added `HardwareFocusTests` + InteractionController input tests (in `FrameHistoryTests`) + `VisualizerModel` output-buffer tests + a Screen-panel render assertion; `SpectreDashboardTests` manual output-completion wiring removed.
4. **Process spawning** (fork/exec/PID). Plan: `validated-booping-koala.md`. ✅ **DONE 2026-06-26 (implemented, NOT yet committed) on branch ShellBacklog — `dotnet test` = 454 passing.** All 7 phases (0,1,3,4,5,6,7) landed; the shell forks/execs/waits/setfocuses end-to-end. See the detailed as-built note below.

**Why this order (decided with user):** A disk is I/O, but block storage (synchronous, no process affinity, block transfer) only needs the device *structure*, not the active-process *routing policy* (which is character-device-specific). So the structural half goes before the disk (disk = block device on it), and the risky routing half goes after. Spawning needs both PID and the disk, so it is last. User's instinct ("file r/w is I/O-bound, refactor I/O first?") → refined to "do part of I/O before, part after the disk."

**Bin filesystem disk** (`bin-filesystem-prototype.md`) — now an **end-to-end** effort, not just a prototype: `Bin` storage class (`CSharpOS/Disk/Bin.cs`); disk registered as a **block device**; privileged **ISA instructions DREAD 0x44 / DWRITE 0x45** (block transfer, addresses absolute, trap in User mode); process references a **disk slot** (`Process.ProgramSlot`; file-path ctor kept as auto-staging shim so ~50 existing tests stay green); `IvtLoadProcess` gains a `DREAD` so the disk→RAM image copy is ISA; new entry field `ProcessEntryDiskSlot = 152` (chosen to leave 136–148 for spawning's PID group). Bin locked API: `int Store(byte[])` (first free slot, -1 if full, throws if oversized), `void Store(int,byte[])` (overwrite), `byte[] Load(int)` (defensive copy, throws on free slot), `int GetLength(int)`, `void Free(int)` (zero+free, idempotent), `IsOccupied`, `FreeSlotCount`. Flat-disk backing (one `byte[]`, fixed-size slots + directory).

**Process spawning** (`validated-booping-koala.md`) — ✅ **DONE 2026-06-26 (NOT committed), 454 tests.** As built:
- **Phase 0 — position-independent ISA:** ESP, the saved EIP, and CALL/RET return addresses are all **offsets from the program base** (so fork is a pure memory copy). Touched `InstructionFunctions.Call/Ret` (push/pop base-relative; ESP is an offset → `programBase+esp`), `Hardware.InitializeStackPointer` (ESP=stackTop−instrStart), `CaptureInterruptedContext` (EIP=IP−GetProgramBase), `OsReturn` (computes IP via new `GetProgramBaseFor(level)` BEFORE SetLevel), `EnterKernel`/`Iret` (return IP user-relative, kernel ESP = stackTop−kernelBase), `OperatingSystem.LoadProcess` (seed EIP=0, ESP=stackTop−progAddr). Test churn: converted ~9 context-save/scheduling/syscall assertions to relative (resume IP = `programAddress+savedOffset`, ESP = offset). New `PositionIndependenceTests` proves it.
- **Phase 1 — PID:** entry fields Pid/ParentPid/WaitTarget/ExitStatus at **136/140/144/148** (Hardware.cs); `OsLayout.NextPidOffset=DataBase+44`; `ProcessState.Zombie`, `WaitReason.ChildProcess`; `Process.Pid`; seed NextPid=1; LoadProcess assigns monotonic PIDs.
- **Phase 3 — IvtSpawn (built per user request even though byte-copy goal was already met):** `LoadProcess` now dispatches **one `IvtSpawn`** routine that allocs + DREADs the image + seeds the saved register file (EIP=0, ESP=TotalSize−KernelStackSize), state, and PID — all ISA (C# kept only the kernel-image write + fd seed + PID read-back). **Enabling refactor:** privileged routines got a **scratch stack** (`OsLayout.PrivilegedStackTop`, `SetupPrivilegedStack` sets ESP) so the buddy allocator became a CALL/RET subroutine **`alloc_sub`** (and buddy-free became **`free_sub`**), reused by spawn/fork/exec/halt — enabled by the now position-independent CALL/RET.
- **Phase 4 — FORK (works):** `EmitFork` finds a free slot, copies sizing, `alloc_sub`s the child, ISA word-memcpy of the parent region, copies the register file (no relocation — relative), assigns child PID/ParentPid, seeds child fds=child index, delivers child PID→parent EAX / 0→child EAX, resume_mlfq. `ForkTests`.
- **Phase 5 — EXEC (works):** new `DLEN 0x46` (disk slot length). `EmitExec` frees old region (`free_sub`, entry still holds old size), recomputes newTotal from the entry, reallocs, DREADs the new program **and a disk-staged copy of the kernel image** (`OsLayout.KernelImageSlotOffset`, seeded in `SeedOsData` via `hw.Disk.Store(KernelImage)`), clears registers, keeps Pid/ParentPid, resumes. `ExecTests` incl. fork→child-exec.
- **Phase 6 — WAIT/EXIT/zombie (works):** HLT→`Hardware.Exit(0)`/EXIT 0x37→`Exit(status)`/fault all funnel through shared `exit_body` (free memory → deliver status to a parent blocked in wait → else Zombie if parent alive → else reap; plus **reap own orphaned zombie children** on exit). `EmitWait` reaps an existing zombie immediately or blocks on `WaitReason.ChildProcess`/`WaitTarget`. WAIT 0x36. `WaitTests`.
- **Phase 7 — shell (works):** `SETFOCUS 0x38` is a **C# hardware instruction** (`Hardware.SetFocus` maps PID→slot→`SetActiveProcess`; NOT an ISA routine, since activeProcess is Hardware state). `Programs.Shell()` = IN cmd → FORK → child EXEC(cmd) / parent SETFOCUS+WAIT → loop. Console menu **mode 9** boots it. `ShellTests`.
- **Opcodes:** FORK 0x34/EXEC 0x35/WAIT 0x36/EXIT 0x37/SETFOCUS 0x38, DLEN 0x46. **IVT:** IvtFork=10/Exec=11/Wait=12/Spawn=13, IvtSlotCount=14. **`OsLayout.DataBase` raised 4096→8192** (spawning routines grew OS code to ~4.8KB). OS-data header now: NextPid(+44), KernelImageSlot(+48), ProcessTable(+52).
- **Manual-only:** the live interactive shell in the dashboard (typing a command id, Tab focus) — headless `ShellTests` proves the mechanics; the TUI itself needs a real `dotnet run` mode 9.

**Additional features (independent of the 4-step roadmap; plan files saved):**

**Commit status (2026-06-28, branch `VisualizerImprovements`):** ALL paging work (Phase 2 inc 1/2/3 + Phase 3 fork→COW) is **COMMITTED** — working tree is CLEAN. Tip commit: `b18e756`. **493 passing tests. The paging roadmap is COMPLETE.** `OsLayout.DataBase` was raised 8192→12288 (the paging ISA grew the OS code past 8192).

**Paging Phase 3 — fork → copy-on-write, as built (2026-06-27, decisions via AskUserQuestion):** *data pages only* (code is untranslated, user stack is RAM-home per process — both stay eagerly copied by fork's RAM memcpy), *true copy-on-write* (read-shared, a write traps), *deterministic slots* with 2-way parent↔child sharing. Key invariant making 2-way deterministic correct for ALL fork patterns (incl. the shell's repeated forks): **a process resolves any existing COW before it forks/exits/execs again**, so COW is only ever a clean parent↔child relationship between one fork and the next teardown. As built: `OsLayout` — COW PTE encoding `CowPte(slot)=-(slot+SwapCowBias=4096)` (more negative than any plain swap, so distinct from -1/-2/SwapPte); per-process `CowPartnerBase` table (partner index or -1, seeded -1 in **BuildOsImage** so even hand-seeded test images are safe); `FrameCowField` (frame is a read-only COW share), frame entry now 32 bytes; helpers `CowPte`/`CowSlotFromPte`/`IsCowPte`/`CowPartnerAddress`. `Hardware` — the MMU re-raises the page fault when a **resident COW frame is WRITTEN** (`FrameCow && isWrite` in `TryTranslateData`), reusing `IvtPageFault`; `SeedPageTableIfNew` is COW-aware (a forked child = `CowPartner>=0` seeds data pages `CowPte(SwapSlot(partner,p))`, else private swap); `FrameCow`/`CowPartner` accessors. **ISA** — `EmitPageFault` now branches at entry on the PTE: **resident** → `pf_cow_write` (CALL `pair_resolve`, resume); **non-resident COW** → DREAD the shared slot into a **read-only COW frame** (reads never copy); records `FrameCow`; eviction of a COW frame re-encodes the owner PTE as `CowPte` (not `SwapPte`). New subroutines: `pair_resolve` (resolve one page: copy snapshot to both deterministic slots, de-COW the writer's frame/PTE, free+repoint the partner so it re-faults private), `resolve_cow` (loop all data pages → pair_resolve, then clear both partners; called from fork/exit_body/exec), `cow_share` (convert the forking parent's data pages to COW — mark resident data frames COW, re-encode private-swap PTEs to CowPte). Fork now: `resolve_cow`→`flush_frames`→`cow_share`, set `cowPartner` both ways, **no eager swap-slot copy**. **Two BUGS FIXED while wiring this:** (1) `flush_frames` clobbers R8 (parent entry) — fork reloads it (latent since inc 2, only fires when the parent dirties a frame before forking); (2) `resolve_cow` clobbers EBX — exit_body & exec reload EBX after the teardown calls (else wait-status delivery / exec sizing read a garbage entry). Tests: 2 new in `PagingTests` (`Cow_ChildWrite_StaysPrivate_ParentUnaffected`, `Cow_ParentWrite_StaysPrivate_ChildSeesSnapshot`). **Scope caveat:** like inc 3, the RAM block still reserves the data region (layout simplicity) — COW shares the swap-slot/frame snapshot, demonstrating the mechanism, not reclaiming that RAM.

**Paging Phase 2 increment 3 — as built (2026-06-27, decisions via AskUserQuestion):** *DATA pages → Bin-disk swap* (code fetched untranslated + kernel stack pinned both stay RAM-home), *one swap slot per page* via a *deterministic fixed reservation* (no bitmap allocator — chosen over the dynamic-bitmap option after a follow-up confirm), *eager deep-copy fork*, *all-in-ISA* transfers. `OsLayout`: `SwapSlot(proc,page)=SwapBase+proc*MaxPagesPerProcess+page` (SwapBase=Hardware.DefaultDiskSlots=64, so swap slots sit above image slots); swap region pre-zeroed at boot in `SeedOsData`; non-resident DATA PTE encodes its slot as `SwapPte(slot)=-(slot+SwapPteBias=3)` (so swap PTEs are <=-67, distinct from -1 unmapped / -2 RAM-home); `IsSwapPte`/`SwapSlotFromPte`/`IsDataPage(page,progSize,reqMem)` helpers; frame table gained `FrameSwapField` (swap slot, or -1 for a RAM-home frame), entry now 28 bytes; two appended OS scratch pages `ZeroPageBase` (always-zero DWRITE source) + `SwapScratchBase` (fork transfer buffer). The disk is enlarged to `DefaultDiskSlots + SwapSlotCount`. `Hardware`: `TryTranslateData` faults on `pte <= -2` (RAM-home OR swap); `SeedPageTableIfNew(index,progAddr,progSize,reqMem,extent)` seeds DATA pages swap-backed, code/stack as -2; `FrameSwap` accessor. **ISA `EmitPageFault`** reads the faulting PTE to branch: swap page → `DREAD swapSlot→frame`; RAM page → memcpy home→frame; records `FrameSwap`; eviction branches likewise (swap victim → `DWRITE frame→slot` + owner PTE := `SwapPte`; RAM victim → memcpy + owner PTE := -2). `flush_frames` got the same swap/RAM branch. New ISA subroutine `zero_swap_slots` (DWRITE the zero page into all of the current process's swap slots) called from `exit_body` + `exec` so a reused slot never serves stale data; `fork` deep-copies parent→child swap slots (DREAD parent slot → scratch → DWRITE child slot) after the swap-aware flush. **BUG FIXED (latent since inc 2):** `flush_frames` clobbers R8, but `EmitFork` needs R8=parent entry for `ForkCopyField` — only triggered when the parent has a dirty frame at fork time (no prior ForkTest wrote data/stack before forking, so it never fired); fork now reloads R8 after the flush call. Tests: 4 new in `PagingTests` (swap-backed seeding, frame tagged with swap slot, dirty data round-trips through the disk slot, forked child inherits parent data via swap). NB: multi-process output tests must call `RaiseOutputComplete(e.Device)` (a forked child outputs on its own device).

**Paging Phase 2 increment 2 — as built (2026-06-27, decisions made via AskUserQuestion):** *ISA fault handler* + *separate frame table (core map)* + *block-home-in-RAM backing* (frame pool is a cache over each page's block home; disk swap stays deferred to inc 3). `OsLayout`: a small shared **frame pool** (`FrameCount=4` PageSize frames at `FramePoolBase`) + a **frame table** (`FrameTableBase`, 24-byte entries: Occupied/OwnerProc/OwnerPage/Home/Dirty/LastUse), both appended after the page tables (only `TotalSize` grows); `FrameTableEntry(f)`/`FrameBase(f)` helpers. A **resident PTE now holds the frame's physical base** (was the page's home in inc 1). **C# MMU** (`Hardware.TryTranslateData`) stamps `LastUse=++pageClock` and sets the dirty bit on each resident access (the reference/dirty bits real MMUs keep in hardware); `StampFrame` + `Frame*` accessors + `ResidentFrameCount`. **ISA `EmitPageFault`** rewritten: scan for a free frame → else pick the **LRU victim** (min LastUse) → if dirty **write it back** to its home (PageSize word-copy) → flip the victim's owner PTE to `NonResidentPage` → copy the faulting page from its home into the frame → record the core-map entry → point the PTE at the frame → `resume_mlfq`. **Coherence:** new ISA subroutines `release_frames` (free the current process's frames, no write-back — called from `exit_body` AND `EmitExec` so a dead/replaced process can't have a stale frame evicted into freed RAM) and `flush_frames` (write back the current process's dirty frames to their homes — called from `EmitFork` before its flat memcpy, so the child copies live parent data). New emit helpers `FrameEntryAddress`/`FrameBaseAddress`/`EmitPteAddress`/`EmitPageCopy`. Each instruction touches ≤1 data page, so a single fault never needs 2 frames → `FrameCount` as low as 1 is correct (4 chosen to force eviction in demos/tests while staying fast). Fully transparent: all prior end-to-end tests green (they exercise eviction+release+flush under fork/exec/exit churn since the global pool is only 4 frames). Tests: 5 new in `PagingTests` (eviction caps at pool size, LRU recency protects a frame, dirty write-back round-trips a value through eviction+reload, dirty-bit set-by-write/clear-by-read, PTE maps into the pool). **Increment 3 (next): Bin-disk swap backing (home becomes a swap slot, DREAD/DWRITE) + load-path / fork-exec rework. Phase 3: fork→COW.**

- **Branch prediction** (`branch-prediction.md`) — ✅ **IMPLEMENTED 2026-06-27 (branch `HardwareUpdate`, not yet committed; 474 tests).** 2-bit saturating BHT (`CSharpOS/CPU/BranchPredictor.cs`, 64 entries, cold=WeakNotTaken, predict-taken≥2) over JZ/JNZ/JS/JNS; observational `cycles` counter + `MispredictPenalty=3` (does NOT drive MLFQ quantum); `BranchPredicted` event; visualizer run-stats/summary show branches/acc/miss/cycles. **Scores user-mode branches ONLY** (`RecordBranch` gated on `level==User` — excludes OS routines AND the shared syscall handler). Behavior-preserving (all prior tests green). Tests: `BranchPredictorTests` (15). RET return-prediction = noted follow-up.
- **Paging** (`paging-demand-swap.md`) — virtual memory; LARGE/invasive, phased. **Phase 1 ✅ DONE 2026-06-27 (branch `HardwareUpdate`, not committed; 483 tests).** Decisions taken: *translate over contiguous buddy blocks* (frame-allocator rework deferred to Phase 2) + *page tables in the OS region* + `PageSize=256`. As built: `OsLayout` page-table region appended AFTER the privileged stack (existing offsets unchanged, only `TotalSize` grows); `PageSize`/`MaxPagesPerProcess=64`/`PageTableAddress(i)`/`UnmappedPage=-1`. **PTEs store the page's PHYSICAL BASE address, not a frame number** (exact for any alignment — the OS region isn't 256-aligned, so frame-number PTEs were off-by-(TotalSize%256)). `Hardware.TranslateDataAddress` (user→page-walk via `TranslateUserPage`; kernel/non-OS→absolute), `SeedPageTable` (linear, raw writes) called in `SetLayoutFromEntry` (universal pre-resume gate → covers loaded/forked/exec'd); LOAD/STORE/CALL/RET data/stack routed through it (code fetch stays physical). Behavior-preserving. Tests `PagingTests` (9) incl. non-linear-PTE remap proving the MMU is real. **Phase 2 = 3 increments, phase-by-phase per user.** Inc 1 ✅ **DONE 2026-06-27 (482 tests)**: demand fault-in via an **ISA page-fault handler** (`IvtPageFault=15`, `IvtSlotCount`→16, `CodeBase` 60→64); `NonResidentPage=-2` sentinel; `TryTranslateData(v,isWrite,out)` replaces the LOAD/STORE/CALL/RET data seam (abort+retry on fault); `RaisePageFault` rewinds IP + dispatches with page in EAX; `SeedPageTableIfNew` seeds pages NonResident once-per-process (guarded by `pageSeedBase`/`pageSeedExtent`); ISA `EmitPageFault` SAVEREGS→set PTE resident (=block home)→`resume_mlfq`. **Decision: inc 1 backs pages by the process's own block (RAM), NOT disk** — a resident PTE = block home (== Phase 1 once resident, trivially coherent); **Bin-disk swap + load-path/fork-exec rework deferred to inc 3.** Transparent (all end-to-end tests green). Inc 2 (next): small frame pool + LRU eviction + dirty write-back. Inc 3: disk swap backing + fork/exec rework. **Phase 3: fork→COW.**

**How to apply:** Step 1 done (committed `2eaae80`); steps 2 (disk) + 3 (focus/console + explicit wake routing) committed by the user; **step 4 (process spawning) done 2026-06-26, NOT yet committed (454 tests)** — the whole 4-step roadmap is now complete. Remaining independent extras: branch-prediction (`branch-prediction.md`, any time) and paging (`paging-demand-swap.md`, depends on the disk; interacts with fork → copy-on-write). Branch-prediction + paging are independent extras (paging after the disk). Six plan files under `~/.claude/plans/`: io-device-abstraction, bin-filesystem-prototype, io-active-process-routing, validated-booping-koala, branch-prediction, paging-demand-swap. Re-read the plan + verify current code before trusting offsets/opcodes. [[project-csharpos]] [[feedback-codestyle]] [[feedback-design-decisions]]
